package com.example.mastermind

// So I can return two things
// May not keep this when I turn the assignment in, but I wanted to make smaller methods that handle more in depth parts of my app
// Easier to read, I think?
data class Counts(
    var correctCount: Int,
    var outOfOrderCount: Int
)
